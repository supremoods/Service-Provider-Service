## How to use this PR's functionality

(Text in parentheses is guidance for what to write in each section - delete the parentheses and write your own text.)

(Add some info about how to trigger the new functionality, e.g. if there are 3 new commands, what order should they be run in? Is there any special set-up needed in the database (such as new columns, adjusted types, ...etc.)? Are any new env settings or special commands needed?)

## Confirm what has been done

(Add any notes about what was done)
(Add notes about why any non-obvious or difficult-at-the-time decisions were made, to give context to the reviewer.)
(Add any notes about not-yet-done aspects of this issue, so the reviewer can decide if they need to be done before merging.)

(Checkboxes below may not all be checked - but it helps the reviewer to know what has (not) been done.)
### Tidy up:
- [ ] Checked ticket requirements and confirmed that PR solves it
- [ ] All meaningful TypeScript/IDE warnings have been cleared
- [ ] Function and variable names are meaningful and reflect the current state of the code
- [ ] Code has been refactored where needed to make it easier to understand and maintain

## Confirm what else needs to be done before the code goes live

(Demonstrate your understanding of what else will be needed before this goes live - e.g. production API keys, permissions, ...etc)
